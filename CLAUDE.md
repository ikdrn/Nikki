# Nikki プロジェクト 開発メモ

## デザイン方針

### カラーパレット — 必ず青ベースで統一すること

色を追加・変更する際は **既存の青（#2563eb / #3b82f6 / #93c5fd 系）を軸** にすること。

- 複数の異なるアクセントカラーを増やさない（多色は見づらい）
- 青以外の色（赤・緑・紫・黄など）をUIに持ち込まない
- 強調が必要なら青の濃淡・透明度で表現する

**現在使用している青トーン早見表:**

| 用途 | カラーコード |
|------|-------------|
| 日付・主要ラベル | `#0284c7` |
| ボタン・インタラクティブ | `#2563eb` |
| カテゴリーラベル（・ラベル） | `#2563eb` |
| フィードバックブロック背景 | `#f0f6ff` |
| フィードバックブロック左ボーダー | `#93c5fd` |
| フィードバックブロック hover 背景 | `#dbeafe` |
| タイトルバッジ背景 | `#e0f2fe` |

### その他のルール

- ランクバッジはランクごとに色を変えない。CSS デフォルト（グレー）で統一
- `rankStyle()` 関数は常に `''` を返す（per-rank カラーは使わない）
- フィードバックカテゴリーラベルは `・ラベル名` 形式（カラフルなピルは使わない）
